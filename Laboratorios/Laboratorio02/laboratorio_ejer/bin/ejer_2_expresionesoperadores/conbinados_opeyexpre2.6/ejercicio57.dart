//57. Verifica si un número es divisible entre 2 y 3 al mismo tiempo.
void ejercicio57 () {
  int numero = 14;
  bool divisible = (numero % 2 == 0) && (numero % 3 == 0);
   print ('El numero $numero es divisible entre 2 y 3 al mismo tiempo? $divisible');

  /*Creamos una variable int y le asignamos el numero 14, despues un bool 'divisible'
  comprovamos si numero es divisible entre 2 o una igualdad a 0 y colocamos un AND y
  si es divisible entre 3 o una igualdad a 0.*/
  
}