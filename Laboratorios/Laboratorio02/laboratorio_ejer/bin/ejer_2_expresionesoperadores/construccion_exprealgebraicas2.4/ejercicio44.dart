//44. Calcula el área de un trapecio con baseMayor, baseMenor y altura.
void ejercicio44() {
  double baseMayor = 20;
  double baseMenor = 14;
  double altura = 5;

  double area = ((baseMayor + baseMenor) / 2) * altura;
   print('El área del trapecio es: $area');

  /*Asignamos valores a las variables baseMayor, baseMenor y altura
  calculamos el área con la fórmula para obtener el área del trapecio
  y se imprime el resultado del área.*/
  
}